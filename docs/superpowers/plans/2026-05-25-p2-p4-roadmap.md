# P2–P4 路线图（主代理维护）

> **2026-05-26 调整**：AI（原 P4）与 P3 深度能力 **冻结**。当前以 [先跑通计划](./2026-05-26-run-first-roadmap.md) 为准。

## P1 ✅（2026-05-25）

- 五轨道合并 `main`
- extension → `@flowweave/recorder`
- studio → `@flowweave/project-knowledge` + login fixture 执行
- `pnpm e2e:login` 端到端通过

## P2：知识库与调试回放 ✅ 主体完成

- [x] `getExecution` / `listExecutions` 从 SQLite 读取
- [x] Studio 展示历史执行、步骤截图路径
- [x] 项目默认环境（baseUrl）入库与 Studio 展示
- [x] Flow 版本历史（`flow_versions` 表 + Web / Studio UI）
- [x] 执行 trace 目录规范（`~/.flowweave/projects/<id>/runs/<execId>/`）
- [x] Studio **按选中 Flow** 运行
- [x] 统一 quickstart / smoke / manual-qa（v1.0.0）

## P3：页面与接口理解 — ⏸ 深度冻结

**已有基础（不扩展）**：DOM 快照 JSON、HAR 落盘、`parseHarSummary`、`analyzeFlowFragility`。

**冻结至智能阶段**：

- [ ] a11y 树、区域标注
- [ ] HAR 与步骤自动关联、请求模板

## P4：AI 编排 — ⏸ 整体冻结

**保留占位**：`@flowweave/ai-orchestrator` 启发式 `generateFlowFromPrompt`（无 UI、无 SDK）。

**冻结项**：

- [ ] Vercel AI SDK 接入
- [ ] NL → Flow Studio 面板
- [ ] 基于知识库的步骤复用建议

## 当前主线（M1–M4）

见 **[2026-05-26-run-first-roadmap.md](./2026-05-26-run-first-roadmap.md)**：

1. M1 本地跑通文档与脚本
2. M2 录制→知识库→回放闭环加固
3. M3 可观测（diff / 截图预览等，按需）
4. M4 CI + smoke

## 产品完整态（远期 P5+）

- 三模式 UI（业务 / 分析 / 专业）
- 协作与多用户（可选）
- 元素自愈、跨站点流程
- **AI 介入**（在跑通稳定后再启动）
