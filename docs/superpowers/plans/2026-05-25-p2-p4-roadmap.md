# P2–P4 路线图（主代理维护）

## P1 ✅（2026-05-25）

- 五轨道合并 `main`
- extension → `@flowweave/recorder`
- studio → `@flowweave/project-knowledge` + login fixture 执行
- `pnpm e2e:login` 端到端通过

## P2：知识库与调试回放

- [x] `getExecution` / `listExecutions` 从 SQLite 读取
- [x] Studio 展示历史执行、步骤截图路径
- [x] 项目默认环境（baseUrl）入库与 Studio 展示
- [ ] Flow 版本历史 UI
- [x] 执行 trace 目录规范（`~/.flowweave/projects/<id>/runs/<execId>/`）

## P3：页面与接口理解

- [ ] `page-intelligence`：DOM 快照、a11y 树、区域标注
- [ ] `network-intelligence`：HAR 捕获与请求模板
- [ ] 脆弱步骤体检（选择器单一 css 警告）

## P4：AI 编排

- [ ] Vercel AI SDK 接入 `ai-orchestrator`
- [ ] NL → Flow 草案（Zod 校验后人工确认）
- [ ] 基于知识库的步骤复用建议

## 产品完整态（P5+）

- 三模式 UI（业务 / 分析 / 专业）
- 协作与多用户（可选）
- 元素自愈、跨站点流程
